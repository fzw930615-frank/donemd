{
  "name": "donemd-windows",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build-debug": "tauri build --debug --no-bundle"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
